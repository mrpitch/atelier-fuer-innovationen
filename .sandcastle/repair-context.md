Independent review found issues after the implementation was committed:

Standards axis (introduced by this diff, commit `31340c8` "cleaup skills and sandcastle"):

1. `.sandcastle/.gitignore` deleted (line 34 of the diff) — it previously ignored `.env`, `logs/`, `container-cache/`, `base-branch.txt` — with no equivalent rule added to the root `.gitignore`. Confirmed live: `git status --short` shows `.sandcastle/logs/` (containing full agent-transcript log files) as untracked (`??`) right now. A routine `git add -A`/`git add .` will commit these Sandcastle session transcripts — which can carry prompts or other sensitive content — into permanent git history.

2. `scripts/set-project-status.sh` is deleted by this diff, but it is still referenced as a live, callable script in three surviving files: `.claude/settings.json:14` and `.claude/settings.local.json:7` (permission allow-list entries `Bash(./scripts/set-project-status.sh *)`) and `docs/agents/implementation-workflow.md:28` (`project_status_command: ./scripts/set-project-status.sh {issue} {status}`). Confirmed the file no longer exists on disk (`ls` returns 'No such file or directory'). The next GitHub Project status-sync attempt will fail.

3. `.agents/skills/setup-my-skills/SKILL.md` survives this diff untouched, but 6 of the 8 seed templates it links to and instructs the reader to copy from were deleted by this same diff: `./templates/issue-tracker-github.md` (lines 87, 204), `./templates/sandcastle/run-sandcastle.ts` (lines 100, 105, 210), `./workflow/implementation-workflow-epics.md` (lines 117, 208 — the template always written to `docs/agents/implementation-workflow.md`), `./templates/triage-labels.md` (line 205), `./templates/domain-diataxis.md` (line 206, the default domain-docs template), and `./templates/domain-context-adr.md` (line 207, its fallback). Only `./templates/sandcastle/sandcastle-dockerfile` and the already-flagged `set-project-status.sh` complete the set of links in that file — meaning the skill is now unusable as documented for most of its offers.

All three stem from a `cleaup skills and sandcastle` sub-commit bundled into this content-migration PR (#121) that mass-deleted ~30 unrelated `.agents/skills/*` and `.claude/skills/*` directories plus supporting scripts — out of scope for issue #100 and leaving dangling references in files the diff didn't also clean up.
